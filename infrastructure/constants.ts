export enum COMMANDS {
  INIT = 'INIT', // init user
  MSG = 'MSG', // message
  CHR = 'CHR', // change room
  LSU = 'LSU', // list users
  HST = 'HST', // history (all messages),
  ERR = 'ERR',
  END = 'END', // only for client
}
